
/**
 * GLOBAL APPLICATION CONFIGURATION
 * Единый файл настроек для всего приложения.
 * Изменяйте значения здесь, чтобы управлять размерами, шрифтами и поведением.
 */

export const APP_CONFIG = {
    // --- НАСТРОЙКИ ИНТЕРФЕЙСА (РАЗМЕРЫ) ---
    layout: {
        /** Высота верхней шапки (Header) в пикселях */
        headerHeight: 50,
        
        /** Высота области таймера (основные цифры) в пикселях */
        timerDisplayHeight: 104,
        
        /** Высота нижней панели кнопок управления (Start/Stop) в пикселях */
        controlButtonsHeight: 96, // Эквивалент h-24
        
        /** Пропорция разделения таймера и статистики (0.65 = 65% ширины под таймер) */
        timerWidthRatio: 0.65,

        /** Высота прогресс-бара внизу таймера в пикселях */
        progressBarHeight: 4,
    },

    // --- НАСТРОЙКИ ТИПОГРАФИКИ (ШРИФТЫ) ---
    typography: {
        // Главный таймер (секундомер)
        timer: {
            /** Единица измерения размера шрифта (vw - процент от ширины экрана) */
            unit: 'vw',
            /** Размер шрифта по умолчанию */
            defaultSize: 19,
            /** Минимально допустимый размер шрифта */
            minSize: 10,
            /** Максимально допустимый размер шрифта */
            maxSize: 35,
            /** Шаг изменения размера при жесте */
            step: 1
        },
        
        // Панель статистики (StatsPanel)
        stats: {
            /** Размер шрифта значений по умолчанию (px) */
            defaultSize: 36,
            /** Минимальный размер шрифта (px) */
            minSize: 16,
            /** Максимальный размер шрифта (px) */
            maxSize: 80,
            /** Множитель масштаба для ячеек (0.8 = шрифт в ячейке на 20% меньше базового) */
            cellScaleFactor: 0.8,
            /** Шаг изменения размера (px) */
            step: 2
        },

        // Таблица логов (List View)
        list: {
            /** Базовый размер шрифта таблицы (px) */
            defaultSize: 16,
            /** Минимальный размер (px) */
            minSize: 16,
            /** Динамический расчет максимума: (Ширина экрана / делитель) + смещение */
            maxSizeDivisor: 18,
            maxSizeOffset: 9
        }
    },

    // --- НАСТРОЙКИ ГРАФИКА (CHART) ---
    chart: {
        /** Отступ тултипа (всплывашки) от пальца по вертикали вверх (px) */
        tooltipVerticalOffset: 70,
        /** Отступ графика сверху (px) */
        marginTop: 40,
        /** Базовое количество точек для отображения в окне графика (если 0 - показывает все) */
        defaultWindowSize: 0, 
        /** Минимальное количество точек при зуме */
        minWindowSize: 3
    },

    // --- НАСТРОЙКИ ВЗАИМОДЕЙСТВИЯ (INTERACTION) ---
    interaction: {
        /** Время удержания кнопки для срабатывания "Long Press" (мс) */
        longPressDelayMs: 800,
        /** Задержка перед началом быстрого изменения значений при удержании жеста (мс) */
        gestureStartDelayMs: 200,
        /** Интервал обновления значений при удержании жеста (мс) */
        gestureIntervalMs: 50,
        /** Таймаут сброса снапшота (заморозки значений) после круга (мс) */
        snapshotFreezeTimeMs: 1000,
    },

    // --- ЦВЕТОВЫЕ ПОРОГИ (ЛОГИКА) ---
    colors: {
        /** Порог прогресс-бара для предупреждения (0.9 = 90%) - Желтый */
        progressWarningThreshold: 0.9,
        /** Порог прогресс-бара для переполнения (1.0 = 100%) - Красный */
        progressDangerThreshold: 1.0
    }
};
